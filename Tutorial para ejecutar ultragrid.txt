Tutorial para ejecutar ultragrid: 1 descargar e instalar ultragird https://www.sitola.cz/igrid/index.php... 2 ir carpeta aplicaciones 3 buscar ultragird y le damos click derecho y seleccionar contenido del paquete 4 abrir la carpeta contenidos 5 ir a la carpeta MacOS 6 y en esa carpeta aparecen varios archivos ejecutables 7 abrimos una ventana en terminal y escribes cd y 8 arrastrar cualquier archivo de los que est‡n al la carpeta de ultragrid que buscamos previamente 9 al llevar el archivo se debe eliminar al ultima parte de la direcci—n incluyendo el slash que la precede, o sea , el final de la l’nea debe ser /MacOS y ahora si le damos enter 10 en este punto debe aparecer al lado del nombre del equipo el nombre de la carpeta que buscamos en el finder 11 copiamos este c—digo en terminal: ./uv -t help con este aparecen los dispositivos de captura disponibles, al copiar las l’neas de c—digo se debe respetar los espacios y todos los signos que aparece 12 si la c‡mara que usas es la interna de un mac esta funcionara con el codec avfoundation que aparece en la lista que te proporciona el help 13 ahora aplicamos la siguiente orden: ./uv -t avfoundation:help con la cual nos mostrara el el puerto por el cual captura la c‡mara y las posibilidades de captura de la misma 14 ahora para comprobar el funcionamiento de la c‡mara ejecutamos un loop con la siguiente orden: ./uv -t avfoundation:0:0 -c libavcodec -d gl con este c—digo se genera un loop nota: el -c es la compresi—n y libavcodec es el que la realiza 15 ahora para tener conexi—n con un punto remoto debemos abrir una ventana nueva de terminal repetir en es los pasos del 2 al 10 y ya con prueba de que el set local funciona aplicamos el siguiente c—digo ./uv -d gl -t avfoundation:0:0 -c libavcodec 200.21.104.4 la ip solo es un ejemplo, debe ir la ip del punto al que queremos enviar el video para que el c—digo sea efectivo se debe correr el ultragrid en ambos puntos